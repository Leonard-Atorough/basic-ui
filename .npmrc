# Strict engine checking (only for pnpm)
# npm will ignore this, yarn will respect it
engine-strict=true
auto-install-peers=true

# Peer dependency handling
strict-peer-dependencies=false

# Share hoisted packages across monorepo
hoisted-node-modules=true
